---
title: vrchat
date: 2026-02-03
tags: [vrchat]
head:
  - - meta
    - name: perspective
      content: tinf
  - - meta
    - name: perspective
      content: tinf
---

有关vrchat一些

---

等下我在思考avator和background多平台支持和优化这块..

基础概念 依旧传统管线:
应用端
图元
纹理(。我不想在见到你都。)
纹素
顶点数组
顶点缓冲区
顶点着色器
片元着色器


现实情况:


OpenGL 1.0 OpenGL ES 1.1
OpenGL 2.0 

ES1.0
OpenGL ES 2.0 / 3.0
srp？
OpenGL 3.0我怎么感觉naninovel....??
DirectX

。
虽然很幽默但是依然因为vrchat去学os。

gdb直接能进linux的c语言环境...?然后数值溢出

Int
Float...处理？、
我将收回对灯光师的一切不当言论(是)

以及记录一些不是很能理解的，
然后灯光貌似是可以烘焙弄进去的(也就是说游戏觉得接收不到光？，但完全不清楚优化的说)

不知道为什么同一个东西要渲染两次。

light部分
体积光
体积云
打光
各种光等

安卓端，不知咋办。
蛟龙adreno对应ES版本型号
继续拼好饭。

glBufferData是一个专门用来把用户定义的数据复制到当前绑定缓冲的函数。它的第一个参数是目标缓冲的类型：顶点缓冲对象当前绑定到GL_ARRAY_BUFFER目标上。第二个参数指定传输数据的大小(以字节为单位)；用一个简单的sizeof计算出顶点数据大小就行。第三个参数是我们希望发送的实际数据。

第四个参数指定了我们希望显卡如何管理给定的数据。它有三种形式：

通过顶点缓冲对象(Vertex Buffer Objects, VBO)管理这个内存
把顶点数据储存在显卡的内存中，用VBO这个顶点缓冲对象管理。

备忘Mtoon
。没有人类了已经，防止内耗的方式是抄代码。

